version: '3.8'
services:
  cli-proxy-api:
    build: .
    container_name: cli-proxy-api
    ports:
      - "80:8317"
    volumes:
      - cli-proxy-data:/app/data
    restart: always

volumes:
  cli-proxy-data:
